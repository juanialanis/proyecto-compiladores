Program {
  integer print_int(integer i) extern;

  void main() {
    integer x;
    integer y;
    integer fact;
    fact = 1;
    y = 3;
    x = 0;
    while (x<y)
    {
      print_int(x);
      x = x+1;
      fact = fact*x;
    }
    return fact;
  }
}
