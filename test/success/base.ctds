Program {
  integer print_int(integer i) extern;

  integer sum(integer i) 
  {
    if( i == 2) then {
      return 100;
    } else {
      return 1;
    }
    return 50;
  }

  void main() {
    integer x;
    x = 2;
    x = sum(x);
    print_int(x); 
  }
}
