Program {
  integer global;
  integer sum(integer a, integer b){
    return a + b;
  }

  void main() {
    integer x;
    integer y;
    integer z;
    global = 8;
    x = global + 2;
    y = 1;
    z = 1;
    x = sum((x+x),y);
  }
}
