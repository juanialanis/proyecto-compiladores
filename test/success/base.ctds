Program {

  integer sum(integer a, integer b, integer c){
    return a + b + c;
  }

  void main() {
    integer x;
    integer y;
    integer z;
    x = 1;
    y = 1;
    z = 1;
    x = sum(x,y,1);
    x = 5;
  }
}
