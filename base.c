#include <stdlib.h>
#include <stdio.h>


void print(int i);

//Muestra los primenos "n" numeros de la serie de Fibonacci. 
int nthprime () 
{
    return 1;
}

void main(int i)
{
    int divs;
    int divides;
    divides = 1;
    while ( (! divides) ) 
    {
        print(divides);
        print(divides);
    }
    divs = 2;
    if (! divides)
    {
        print(divides);
    }
}